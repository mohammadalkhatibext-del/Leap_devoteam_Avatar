# Arabic TTS. Both are needed by server/tts_service.py and scripts/render-edge-tts.py.
#
# edge-tts  — reaches Microsoft's neural Arabic voices (ar-SA-HamedNeural and friends)
#             with no API key and no credit card. Same models Azure Speech serves, so
#             quality carries over exactly; see the provenance note in tts_service.py
#             before shipping this path.
# miniaudio — decodes the MP3 edge-tts returns into raw PCM. Chosen over pydub/ffmpeg
#             because it needs no ffmpeg binary on PATH, which this machine lacks.

edge-tts>=6.1
miniaudio>=1.59
