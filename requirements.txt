# Python side of the pipeline. Both are used by two things:
#   server/tts_service.py       — the TTS sidecar the booth actually speaks through
#   scripts/render-edge-tts.py  — renders the Phase 0 fixture audio
#
# edge-tts reaches Microsoft's neural voices with no key and no card. It is the same
# model Azure sells as ar-SA-HamedNeural, but it is an undocumented endpoint:
# fine for building and for the bake-off, must not be what runs on the booth floor.
edge-tts>=7.0.0
miniaudio>=1.59
